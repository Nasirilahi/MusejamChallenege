import {
    Navigator,
    StyleSheet,

} from 'react-native';

const styles = StyleSheet.create({
    centering: {
        alignItems: 'center',
        justifyContent: 'center',
        padding: 8,
    },
    gray: {
        backgroundColor: '#cccccc',
    }
});
export default styles;